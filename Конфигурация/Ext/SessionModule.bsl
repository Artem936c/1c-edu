// Модуль сеанса. Устанавливает параметры сеанса при старте.

Процедура УстановкаПараметровСеанса(ТребуемыеПараметры)

    // ТребуемыеПараметры = Неопределено — инициализация при старте сеанса.
    // Единственный параметр сеанса конфигурации — ТекущийПользователь.

    УстановитьПривилегированныйРежим(Истина);
    ПараметрыСеанса.ТекущийПользователь = ОбслуживаниеПользователейВызовСервера.ТекущийПользователь();
    УстановитьПривилегированныйРежим(Ложь);

КонецПроцедуры
